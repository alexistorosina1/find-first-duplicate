function findFirstDuplicate(arr) {
    // let newArr = new Set();

    // for (let i = 0; i < arr.length; i++) {
    //     if (newArr.filter (arr[i]))
    //         return newArr;
    //         newArr.add(arr[i]);
    // }
    // return "No duplicates found"

    
}